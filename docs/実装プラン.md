# 実装プラン：PROXY CHAMPIONS

> 企画書（`企画書.md`）の実装ロードマップ。
> 作成日: 2026-07-04 / ステータス: M0・M1 完了（契約＋戦闘エンジン＋決定論テスト6件green）・M2着手待ち
> 対象スコープ = MVP（個人戦・派遣ダンジョン・4魔法ライン・CTB戦闘）。団体戦は将来（企画書8章）。

---

## 0. 全体戦略（なぜこの順番か）

**エンジンファースト × インフラ前に検証。**

- 戦闘エンジンは**純粋関数**（`battle(A, B, seed) → {winner, eventLog}`）＝**インフラ0で作れる**。しかも**最大の難所＝バランス**（企画書9章）。
- よって **UI・DB・大会を積む前に、エンジンとバランス検証（M0〜M2）を先に終える**。核が正しいと分かってから周辺を乗せる。
- もし三すくみが崩れる／最強ステ・死にステが出る等の問題があれば、**UIを1画面も作る前に安く直せる**。インフラを積んだ後に気づくと高い。
- 決定論（企画書13.4）ゆえ、M2の総当たりシミュは何千試合でも一瞬＝**launch前に9章のlive-ops調整を予行できる**のが最大の武器。

**基本原則**：各マイルストーンは「**観測できる成果物**（動く／テストが通る）」で締める。

---

## 1. マイルストーン一覧（依存順）

| # | 目標 | 成果物（観測できるもの） | 依存 |
|---|---|---|---|
| **M0** | 契約を固める | ①ビルド入力スキーマ ②eventLogスキーマ ③戦闘定数config（仮値） | — |
| **M1** | 戦闘エンジン（純粋関数） | TS `battle()`＝CTBループ・シード固定RNG・物理/魔法式・4ライン・自動判断AI・単体テスト | M0 |
| **M2** | バランス検証ハーネス | 総当たり×多シードの勝率マトリクス／ヒートマップ | M1 |
| **M3** | 永続化基盤（Supabase） | テーブル群＋Auth（匿名→昇格） | M0 |
| **M4** | 薄い縦スライス（E2E 1本） | キャラ作成→即時1戦→battle()→ログ保存→Flutter再生 | M1,M3 |
| **M5** | 育成ループ（派遣ダンジョン） | 派遣・体力ループ・自然回復・回復薬・ステ振り・装備 | M4 |
| **M6** | 個人戦（バッチ大会） | pg_cron一斉シミュ・ディビジョン・シーズン・プッシュ | M5 |
| **M7** | 課金・仕上げ・ストア | 課金/広告/アバター・ストア公開 | M6 |

### クリティカルパス
```
M0 → M1 → 【M2＝設計の正否を判定】 → M3 → M4 → M5 → M6 → M7
```
### 並行可能
- **Flutter画面ガワ**（メニュー/ステ振りUI/ショップ/ログ表示）：M0のスキーマ確定後、M1・M2と並行着手可。
- **アバター/Storage**（企画書3.7）：独立。M7内だが前倒し可。

---

## 2. マイルストーン詳細

### M0 — 契約を固める（設計・実装前）
エンジン／DB／Flutterの**共通契約**を先に確定する。CTBが決まった今、ほぼ機械的に書ける。

**成果物**
1. **ビルド入力スキーマ**（`battle()` の入力＝1キャラのビルド。DB保存形・クライアント入力形と同一）
2. **eventLogスキーマ**（`battle()` の出力＝再生の契約）
3. **戦闘定数config**（倍率・±%・THRESHOLD・逓減カーブ。**全部仮値でよい**。後でM2/9章で実測調整）

**ビルド入力スキーマ（ドラフト・仮）**
```jsonc
{
  "characterId": "uuid",
  "level": 12,
  "stats": {            // 基本5ステ（1プールから配分。同スケール／企画書3.5.1）
    "vit": 10,          // 体力  → maxHP = vit × 10（仮）
    "mag": 8,           // 魔力  → maxMP = mag × 5（仮）＋魔法威力/軽減/状態異常成功率
    "pow": 6,           // 力    → 物理攻撃
    "spd": 9,           // 素早さ→ CTB持ち点の溜まり＋物理回避
    "men": 5            // 精神  → 状態異常耐性（のみ）
  },
  "spellLines": {       // 魔法ライン修行値（同じ1プールから／企画書3.5.2）
    "fire": 20,         // Tier = floor(value/10) 等（仮）
    "cure": 10,
    "sleep": 0,
    "strength": 0
  },
  "equipment": {        // ゴールド軸＝横の型（企画書3.6）
    "weapon": "sword_iron",   // 力型/魔法型など「型」
    "armor":  "mail_leather", // 物理防御の担い手
    "shield": null
  }
}
```

**eventLogスキーマ（ドラフト・仮）**
```jsonc
// battle() が返すイベント列。Flutterはこれを順に再生するだけ（企画書13.1）。
[
  { "t": 0, "type": "battle_start", "combatants": ["A","B"] },
  { "t": 3, "type": "gauge_ready",  "actor": "A" },
  { "t": 3, "type": "attack",  "actor": "A", "target": "B", "weapon": "sword_iron",
    "damage": 42, "crit": false, "hpAfter": 118 },
  { "t": 5, "type": "cast",    "actor": "B", "spell": "fire", "tier": 2, "mpAfter": 12 },
  { "t": 5, "type": "damage",  "actor": "B", "target": "A", "amount": 55, "kind": "magic", "hpAfter": 90 },
  { "t": 7, "type": "cast",    "actor": "A", "spell": "sleep", "tier": 1 },
  { "t": 7, "type": "status_apply", "target": "B", "status": "sleep", "duration": 2, "success": true },
  { "t": 9, "type": "miss",    "actor": "B", "target": "A", "reason": "evade" }, // 物理のみ
  { "t": 12,"type": "cast",    "actor": "A", "spell": "cure", "heal": 40, "hpAfter": 130 },
  { "t": 20,"type": "ko",      "target": "B" },
  { "t": 20,"type": "battle_end", "winner": "A", "seed": 123456 }
]
```
> `t` はCTBの内部時刻/行動カウンタ（企画書4.1.1）。イベント種別は実装で確定。

**完了条件（DoD）** ✅ 完了
- [x] 3スキーマをファイル化（`engine/src/schema.ts`＝型・`formulas.ts`＝仮値config＋装備カタログ）
- [x] 「5ステ＋4ライン＋装備3枠」で1ビルドが過不足なく表現できる（`CharacterBuild`）
- [x] eventLogの全種別が「攻撃/魔法/回復/状態異常/回避/撃破/開始終了」を再生に必要な最小情報で覆う（12種の discriminated union）

---

### M1 — 戦闘エンジン（純粋関数・インフラ0）★最優先
**実装言語＝TypeScript**（Edge Function前提・企画書13.9／将来ワーカー移設も見据える）。グラフィック無し・計算のみ。

**実装内容**
- **CTB持ち点ループ**（企画書4.1.1の擬似コード）：`combatants` を**リスト対リスト**で書く（団体戦へ無改修拡張）。1v1はN=1。
- **シード固定RNG**（決定論。同シード→同結果）。試合ごとに新規シードを外から受け取る。
- **物理ダメージ式**（力×武器・鎧で軽減・素早さで回避・±10〜15%乱数）
- **魔法ダメージ式**（`Fire基本＋術者魔力−受け手魔力`・下限あり・%最大HP成分・回避不可）
- **4魔法ライン**：Fire（攻撃）／Cure（回復）／Sleep（状態異常）／Strength（バフ）
- **状態異常（sleep）**：成功率＝魔力(攻) vs 精神(防) の相対（floor/ceiling・0/100%なし）・持続限定・被弾で起きる・再睡眠に逓減
- **自動判断AI（定石）**：`HP50%未満→Cure／開幕→Strength／強敵→Sleep／通常→Fire`（企画書4.2・仮ロジック）
- **ターン/MP上限**（PvP膠着防止）

**完了条件（DoD）** ✅ 完了（`engine/src/battle.ts`・`rng.ts`／テスト6件 green）
- [x] `battle(input, seed)` がCLIで走り、eventLogを出力（`deno eval` で確認済）
- [x] **決定論テスト**：同 (input, seed) が必ず同結果・同ログ（JSON完全一致で検証）
- [x] 境界テスト：低魔力が魔法に溶ける／sleepは低精神に効き高精神に効きにくい（魔力≈精神で50%）／非魔法型はcast0・attackのみ／大半の試合で決着
- [x] `gaugeGain` の逓減（sqrt）で **2倍速≠2倍手数**
- [ ] 残：MP切れで魔法型が失速・回復型が長く粘る の明示テストは M2 ハーネスで勝率と併せて確認

---

### M2 — バランス検証ハーネス ★最高レバレッジ
純粋関数の上に、**launch前にバランスを実測する**スクリプトを載せる。UI・インフラ不要。

**実装内容**
- 代表ビルド群（脳筋物理／魔法使い／回復僧侶／速攻／タンク／睡眠パンチャー等）を定義
- **総当たり × 数千シード**で `battle()` を回し、勝率を集計
- 出力：**勝率マトリクス／ヒートマップ**（CLI表 or 簡易HTML）

**検証項目（企画書の設計が創発しているか）**
- [ ] **三すくみが数字から創発**（魔法使い＞タンク＞速攻物理＞魔法使い の輪／企画書9章）
- [ ] **勝率ターゲット**：同格45〜55%・1ティア格上65〜70%・明確格上80%・**100%が無い**（企画書4.1.2）
- [ ] **死にステ・最強ステが無い**（全ステに役割／企画書3.5）
- [ ] **スリープのアップセット率**が「希望はあるが理不尽でない」範囲／精神で自己修正メタが効く
- [ ] 崩れていれば **M0のconfig（仮値）を回して調整** → 再シミュ（この往復が安い＝ここでやり切る）

---

### M3 — 永続化基盤（Supabase）
**テーブル設計（企画書13.9）**
- `players`（Auth連携・匿名→昇格）
- `characters`（＝ビルド。M0スキーマをそのまま列/JSONBに）
- `equipment`（所持装備・型）
- `dungeons`（タイプ・難易度・推奨潜航時間・ドロップ表）
- `tournaments` / `matches`（結果＋eventLog保存）/ `divisions` / `standings`（順位表）

**完了条件（DoD）**
- [ ] マイグレーションでテーブル生成
- [ ] Auth：匿名サインイン → 後でメール/ソーシャル昇格
- [ ] キャラの保存・読み出しが通る（M0スキーマと往復可）

---

### M4 — 薄い縦スライス（E2E 1本）★パイプライン検証
**エンジン→DB→Flutter再生**を1本つなぎ、「非同期観戦」の手触りを最速で確認。

**流れ**：1キャラ作成 → 即時オート1戦（企画書3.3.1の即時オート＝練習試合5.3を兼ねる） → サーバーで `battle()` → 結果＋eventLog保存 → **Flutterでログ再生**（黒背景・テキスト主体・被弾で画面揺れ／企画書4章）

**完了条件（DoD）**
- [ ] キャラ1体を作ってDB保存
- [ ] サーバー側で `battle()` 実行しログ保存
- [ ] Flutterがログを取得し**クラシックRPG風に再生**
- [ ] 地味でも「戦って、後で観る」が一気通貫で動く

---

### M5 — 育成ループ（派遣ダンジョン＝縦の本丸）
**大会が無くてもソロで毎日遊べる**状態にする。

- **派遣ダンジョン**：指定時間まで自動連戦（サーバー計算・アプリ閉じてよい）
- **体力ループ**：ダメージで体力減→体力0で強制帰還（そこまでの報酬持ち帰り）
- **自然回復**（時間）＋**回復薬**（即回復）＋即時オート（1戦）
- **成長**：レベルアップ→ステ振り→魔法ライン投資→**リスペック（ゴールド消費）**
- **装備**：ショップ（ゴールド狙い撃ち）／ドロップ／ピティ（天井）
- ソフトキャップ曲線（上限なし・伸び鈍化のみ）

**完了条件（DoD）**
- [ ] 派遣→自動連戦→報酬（XP/ゴールド）→帰還→回復のループが回る
- [ ] レベルアップでステ振り・ライン投資・リスペックができる
- [ ] 装備を店/ドロップ/ピティで入手し「型」を変えられる

---

### M6 — 個人戦（バッチ大会）
コンセプトの完成形＝**非同期観戦大会**。

- **pg_cron → Edge Function** で定時バッチ（企画書13.5）：対戦カード確定 → `battle()` 一斉実行 → 結果/ログ保存 → 勝者を次ラウンド → 順位・昇降格更新 → **冪等性**（処理済みフラグ・トランザクション）
- **ディビジョン**（1〜2階層・昇降格）・**シーズン制**・**予選リーグ→トーナメント**・練習試合・敗者復活
- **FCMプッシュ**「結果が見れるよ」

**完了条件（DoD）**
- [ ] 定時バッチで1シーズンが自動進行（エントリー→対戦確定→ラウンド進行→シーズン終了）
- [ ] 再実行しても二重処理しない（冪等）
- [ ] プレイヤーが後からログを観て順位/昇降格を確認できる

---

### M7 — 課金・仕上げ・ストア
- **課金**：回復薬・時短ブースト・枠追加・シーズンパス・見た目（グラフィック/称号/色/レトロスキン）・動画広告
- **アバター**（企画書3.7）：プリセット（同梱）＋アップロード（256²webp強制圧縮・1キャラ1枚上書き・通報対応）・Supabase Storage
- **不正対策**：ミニゲーム報酬はサーバー検証/上限（企画書13.6）
- **ストア**：Apple Developer 年\$99・Google Play 初回\$25・審査

---

## 3. 技術スタック & ディレクトリ構成

### 3.1 技術スタック（確定・企画書13.8）
- **クライアント**：Flutter（+ Flame：ミニゲーム/戦闘再生）
- **バックエンド**：Supabase（Postgres / Auth / Realtime / Edge Functions(TS) / pg_cron / Storage）
- **プッシュ**：FCM（無料）
- **戦闘エンジン**：TypeScript・**Deno**・純粋関数・決定論・ステートレス・**依存ゼロ**（将来ワーカー移設可）
- **コスト**：無料枠スタート → 増えたらSupabase Pro → 従量（構成不変）

### 3.2 ディレクトリ構成（モノレポ・確定）
関心（engine / supabase / app）でトップ階層を割る。企画書13.1「アプリとサーバーで言語が違ってよい／戦闘エンジンは1回だけ実装」がそのまま指針。

```
proxy_champions/
├── docs/
│   ├── 企画書.md
│   └── 実装プラン.md
│
├── engine/                    # ★TS純粋関数（DBもFlutterも知らない）
│   ├── src/
│   │   ├── schema.ts          # M0: ビルド入力 + eventLog + config型（契約の"正本"）
│   │   ├── formulas.ts        # M0: 戦闘定数config（仮値）
│   │   ├── rng.ts             # シード固定RNG
│   │   └── battle.ts          # M1: CTB戦闘エンジン
│   ├── scripts/
│   │   └── balance.ts         # M2: 総当たり勝率ハーネス
│   ├── tests/                 # 決定論・境界テスト
│   └── deno.json
│
├── supabase/                  # ★DB＋サーバー（Supabase CLI標準レイアウト）
│   ├── migrations/            # M3: テーブル定義（SQL）
│   ├── functions/
│   │   └── run-battles/       # M6: pg_cron発火のバッチ → engine を import
│   └── config.toml
│
├── app/                       # ★Flutter（Dart・標準構成）
│   ├── lib/
│   │   ├── models/            # M4: build/eventLog の Dartモデル（engine契約のミラー）
│   │   ├── features/          # ステ振り/ショップ/派遣/観戦再生 …
│   │   └── main.dart
│   └── pubspec.yaml
│
└── README.md
```

**棲み分けの理屈**
- **`engine/`**＝言語・ランタイム独立。純粋関数でDBもUIも知らない → M0〜M2で最初に単独完成する土台。
- **`supabase/`**＝Supabase CLI標準レイアウト。バッチ（`run-battles`）が engine を import して `battle()` を呼ぶだけ。
- **`app/`**＝Flutter標準。engine契約をミラーしたDartモデルを持ち、サーバーが返す eventLog を再生するだけ。

**言語をまたぐ継ぎ目は2箇所だけ**
1. **engine → Edge Function（同一言語・同一ソース）**：engine を Deno製の素のTS（依存ゼロ・ビルド無し）で書き、`functions/run-battles/` から**同じ `battle.ts` を import**。M2ハーネスも Deno（`deno run scripts/balance.ts`）。＝**ハーネスとバッチが1ソース共有＝企画書13.1「1回だけ実装」**。
2. **engine ↔ Flutter（唯一の非対称点）**：TSの型はDartから import 不可。**正本＝`engine/src/schema.ts`**、Flutterは `app/lib/models/` に**対応するDartクラスを手書き**（線を越えるのはJSON＝eventLog、Dartはデシリアライズするだけ）。ドリフトが問題化したら後でコード生成を足す（**今は入れない**）。

---

## 4. 主要リスクと対策
| リスク | 対策 |
|---|---|
| バランス崩壊（最強ステ/死にステ/一強） | **M2で実測してからUIを作る**。決定論シミュで安く反復 |
| 素早さ（CTB手数）の万能化 | 逓減（2倍速≠2倍手数）＋魔法は回避不可＋1プールで速度型は紙 |
| Edge Functionの計算時間制限（大量試合） | MVPは単純ループで十分／将来は戦闘エンジンだけ外部ワーカーへ（純粋関数で分離済） |
| スキーマ変更の波及 | **M0で契約を先に固定**。以降はマイグレーションで育てる |
| perma-lock（sleep) 特にPvP | 再睡眠に逓減・耐性上昇・持続限定・成功率floor/ceiling |

---

## 5. 今すぐの初手（M0〜M2を先に）
Supabaseも Flutterも要らず、**ローカルのTSだけで完結**。ここで「面白い数式か」がUI前に判る。

1. **M0**：ビルド入力スキーマ ＋ eventLogスキーマ ＋ 戦闘定数config（仮値）をファイル化
2. **M1**：CTBループの骨組み（仮式）＋ 決定論テスト
3. **M2**：総当たり勝率スクリプトを回して手触り確認

---

## 6. 未確定で作りながら決めるもの（企画書12章・13.9由来）
- 戦闘式の具体%（M2で実測調整）／ソフトキャップ曲線の傾き
- 派遣ダンジョンの具体（潜航刻み・回復レート・回復薬効果量・ドロップ設計）
- シーズン長・大会フォーマット・ディビジョン昇降格条件
- 自動判断AIの定石ロジック詳細／sleepの命中・耐性・持続の数値
- 認証方式の昇格フロー／マネタイズ具体ライン
- 武器習熟度（FF2式）の要否
