{
  "tasks": {
    "test": "deno test src/ tests/",
    "balance": "deno run scripts/balance.ts",
    "dive": "deno run scripts/dive_demo.ts",
    "growth": "deno run scripts/growth_demo.ts",
    "season": "deno run scripts/season_demo.ts",
    "check": "deno check src/**/*.ts",
    "sync-edge": "rm -rf ../supabase/functions/_engine && cp -R src ../supabase/functions/_engine"
  },
  "fmt": {
    "useTabs": false,
    "lineWidth": 100,
    "indentWidth": 2,
    "singleQuote": true
  },
  "lint": {
    "rules": { "tags": ["recommended"] }
  }
}
